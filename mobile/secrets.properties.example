# Copy to secrets.properties (gitignored) for local/release builds.
# Never commit secrets.properties.
#
# Kiosk lock-task exit PIN — long-press the brand label in the header.
# Also overridable via STATCO_ADMIN_EXIT_PIN env or -Pstatco.adminExitPin=...
statco.adminExitPin=change-me
