export const environment = {
  production: true,
  /**
   * In production, deploy the frontend behind the same origin as the API
   * (recommended) or configure a reverse proxy to forward /api to the backend.
   */
  apiBaseUrl: '',
};
