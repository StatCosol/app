# Extra deps for EMBEDDING_BACKEND=insightface (ArcFace, 512-d).
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-arcface.txt
#
# Model pack (buffalo_l ≈ 280 MB) is downloaded on first use into
# INSIGHTFACE_HOME (/models/insightface). For air-gapped deploys, bake it
# into the image or mount it — see docs/FACE_KIOSK_UPGRADE.md.
insightface==0.7.3
onnxruntime==1.20.1
